framework:
    validation:
        email_validation_mode: html5
        # En prod, on ne mappe pas automatiquement les contraintes Doctrine,
        # on les déclare explicitement sur les entités.

when@test:
    framework:
        validation:
            not_compromised_password: false
